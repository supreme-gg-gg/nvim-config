# Usage

This is intended for personal usage when moving between laptops (work / personal).

```bash
# brew install nvim
mkdir .config/nvim
cd .config
git clone <this-repo> nvim
nvim
# Run :Lazy sync if needed after nvim starts
```
